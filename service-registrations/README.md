# Service Registration

Servicio base para manejar registros del sistema.

## Scripts

- `pnpm start`: inicia el servidor
- `pnpm dev`: inicia el servidor con reinicio automático

## Variables de entorno

Copie `.env.example` a `.env` y ajuste los valores según su entorno.
